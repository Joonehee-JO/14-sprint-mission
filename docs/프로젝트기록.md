### 리팩토링 기록

1. ChannelServiceApp - makePrivateChannel()
    - 기존 채널 참여 유저 아이디를 리스트로 받아 참여 리드스테이터스를 생성했는데 이 순간에 여기 참여하는 유저가 계정 삭제 메서드를 요청가능하며 존재하지 않는 유저를 대상으로 생성되는 것을 방지하기 위해 유효성검증이 필요하다 생각하여 리팩토링









### fix 기록

1. 유저 삭제 메서드를 실행하는 순간 NPE 터짐
   - 삭제 메서드에서 프로필이미지 널검증을 처리하지 않음을 확인
   - 재차 터짐
     1. 자세히 보니 초기 설계 시 프로필 이미지를 설정하지 않는다면 서비스에서 기본 이미지를 제공하는 것으로 요구사항을 자체 수정함.
     2. 근데 기본 이미지 추가 및 UUID 설정을 임시로 하기 위해 0000 형태로 하드 코딩 해놓음
     3. 이에 따라 널검증을 수행함에도 이미 쓰레기값이 들어있어 널검증에 잡히지 않고 추가로 그 하드코딩된 UUID 로 파일 경로를 찾으려하니 런타임 에러가 나는 것을 확인
     4. 근데 그렇게 가져온 값이 유효하지 않아 결과가 널이였어도 커스텀예외로 잡을 수 있었는데 Optional로 결과를 던질 시 Nullable이 아닌 of 형태로 던지는 것을 확인
        - Optional.of 와 Optional.ofNullable 의 차이를 확인
        
2. 